import UIKit

var str = "Hello, Week-08"

//Cordinate system data structures

//CGPoint to mark 2D space
var cordinates = CGPoint(x: 91.0, y: 70.5)

//CGSize to define shapes with height and width
var rectangleDim = CGSize(width: 200.0, height: 100.5)

//CGRect to define a rectangle shape
let reactangle = CGRect(origin: cordinates, size: rectangleDim)
